#ifndef MEMORY_HANDLING
#define MEMORY_HANDLING
#include "data.h"
void tambah_dokter(Dokter** data_dokter, int* size);
void hapus_dokter(Dokter** data_dokter, int* size);
void ubah_dokter(Dokter* data_dokter);

#endif